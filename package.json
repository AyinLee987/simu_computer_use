{
  "name": "browser-agent-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A screenshot-only AI desktop agent with a Docker browser, noVNC live view, and bounded mouse/keyboard actions.",
  "scripts": {
    "launch": "node scripts/start.mjs",
    "setup": "node scripts/setup.mjs",
    "desktop": "docker compose up -d --build",
    "desktop:stop": "docker compose down",
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": { "node": ">=22" },
  "dependencies": { "playwright": "^1.55.0" }
}
